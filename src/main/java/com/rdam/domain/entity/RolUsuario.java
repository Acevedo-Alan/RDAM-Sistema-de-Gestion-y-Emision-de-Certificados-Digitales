package com.rdam.domain.entity;

/**
 * Enum que mapea el tipo PostgreSQL rol_usuario.
 * Valores en minúscula para coincidir exactamente con el DDL.
 */
public enum RolUsuario {
    ciudadano,
    interno,
    admin
}
